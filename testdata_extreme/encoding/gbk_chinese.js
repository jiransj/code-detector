// GBK 编码典 JavaScript 文件

function 计算总计(数值列表) {
    return 数值列表.reduce((a, b) => a + b, 0);
}

function 获取用户信息(用户ID) {
    const 用户 = {name: "张三", id: 用户ID};
    return 用户;
}

function add(x, y) { return x + y; }
